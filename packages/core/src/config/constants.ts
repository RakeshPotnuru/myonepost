const POST = {
  TEXT_MAX_LENGTH: 4000,
  COMMENT_MAX_LENGTH: 280,
};

export const CONSTANTS = {
  POST,
};

export enum FeedType {
  TRENDING = "trending",
  FRESH = "fresh",
  POPULAR = "popular",
}
